query fetchApplication($appName: String!) {
  md_application(appName: $appName) {
    ... on MD_Application {
      name
      account
      userPermissions {
        id
        writeAccess
        error
      }
    }
  }
}
