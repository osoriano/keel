query fetchVersions($appName: String!, $reference: String!, $environment:String!) {
  md_application(appName: $appName)  {
    ... on MD_Application {
      name
      versionOnUnpinning(reference: $reference, environment:$environment) {
        buildNumber
        version
      }
    }
  }
}
